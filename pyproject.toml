[project]
name = "hermes-model-radar"
version = "0.1.0"
description = "Hermes usage analytics + OpenRouter model radar"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.32",
  "httpx>=0.27",
  "pandas>=2.1",
  "pyyaml>=6.0",
  "python-dotenv>=1.0",
]
